// 11921ECP001
#include <stdio.h>		// Biblioteca para entrada e saida de dados

int main(){
  int a, b;			// Variaveis que armazenam os valores inteiros
  scanf("%d %d", &a, &b);	// Leitura dos valores inteiros nas variaveis a e b
  printf("X = %d\n", a + b);	// Printa o resultado da soma das dois valores lidos
  return 0;			// finaliza a funcao principal
}